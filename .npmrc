# Keep the two Zama SDK generations (@zama-fhe/sdk@3.x frontend vs
# @zama-fhe/relayer-sdk@0.4.1 inside the hardhat plugin) in fully separate
# dependency trees. Never enable hoisting.
node-linker=isolated
shamefully-hoist=false
auto-install-peers=true
strict-peer-dependencies=false
